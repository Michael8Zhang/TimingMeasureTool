Command Line usage:
1. cd build/classes
2. java test.question3.TimingMeasure [inputfile] [outputfile]
3. note: 
3.1 default Class to test specified in inputfile classinfo.txt, 
    if no inputfile use MyTestClass as default Class to test.
3.2 if outputfile not specified, output to testoutput.txt
 

